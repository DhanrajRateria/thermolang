./build/thermolangc examples/star_graph.thermo --target=thrml

python3 benchmarks/run_noise_shaping.py

./build/thermolangc examples/checkerboard.thermo --target=fpga
mv checkerboard_config.mem hardware/fpga/3_ising_solver_config.mem
mv checkerboard_schedule.txt hardware/fpga/3_ising_solver_schedule.txt

cd hardware/fpga
vvp sim_array

cd hardware/fpga
vvp sim_core
gtkwave spu_core_test.vcd

The Climax: Watch the terminal output.
"Step 0... Random."
"Step 1000... Cooling."
"Final State: [-1, 1, -1, 1...]"
The Visual (GTKWave):
Open the spu_core_test.vcd (from the core test).
Zoom in on the rand_out signal.
Say: "This isn't a random number function. These are Ring Oscillators. We are simulating electrons fighting each other in silicon to generate true physical entropy. This is a digital twin of a thermodynamic chip."


Part 4: Visual Assets for the Presentation
To make the "invisible" visible, include these three artifacts in your presentation deck or folder:
The Architecture Diagram:
Left: .thermo Code.
Middle: The Compiler (showing "Noise Shaping Pass" injecting values).
Right: Two outputs. One going to Extropic/GPU, one going to FPGA/Hardware.
The "Money Plot":
The bar chart generated by run_noise_shaping.py. Label it clearly: "Compiler-Optimized Annealing Performance."
The Hardware Logic:
A screenshot of the Verilog spu_core code showing the trng instantiation. This proves you built the "Self-Driving" architecture.



./build/thermolangc --version
./build/thermolangc --help

ls examples
sed -n '1,120p' examples/dense_40.thermo
sed -n '1,160p' examples/denoise_10x10.thermo

./build/thermolangc examples/dense_40.thermo --emit-ir
./build/thermolangc examples/dense_40.thermo --emit-ir --verbose

./build/thermolangc examples/dense_40.thermo --target=thrml --verbose
ls | grep dense_40

python3 dense_40_thrml.py